# MX Pricing — development defaults
DEPLOY_ENV=development
PRODUCTION_MODE=false
AUTH_DRIVER=dev
PORT=3000

# Optional local PostgreSQL (docker compose up -d postgres)
# DATABASE_URL=postgresql://mx:mx@localhost:5432/mx_pricing

# WAF (enabled automatically in staging/production)
# WAF_ENABLED=true
# WAF_RATE_LIMIT_PER_MINUTE=300
